Final polish pass across the entire site. Do this in order:

═══════════════════════════════════════
PART 1 — FULL SPACING & LAYOUT AUDIT
═══════════════════════════════════════

Go through EVERY component and page file one by one. For each file:

1. SPACING SCALE — enforce a consistent spacing system throughout:
   - Base unit: 4px. All padding/margin/gap must be multiples of 4
   - Section vertical padding: py-24 (desktop) / py-16 (mobile) — no section should feel cramped
   - Between section heading and content: mb-12 minimum
   - Between cards in a grid: gap-6 (desktop) / gap-4 (mobile)
   - Inner card padding: p-4 minimum, p-6 for content-heavy cards
   - Never use arbitrary pixel values — use Tailwind's spacing scale only

2. TYPOGRAPHY BREATHING ROOM:
   - All headings: add mb-4 below, never sit flush against the next element
   - Body text line-height: leading-relaxed (1.625) everywhere
   - Paragraph max-width: max-w-2xl for any centered body text block
   - Letter-spacing on uppercase labels: tracking-widest minimum
   - Never let two different font sizes sit with zero gap between them

3. NAVBAR:
   - Announcement bar: py-2.5 px-4, text centered
   - Nav height: h-[68px] exactly, items perfectly vertically centered with items-center
   - Gap between nav links: gap-8 on desktop
   - Right icons: gap-5, all icons same size (19px)
   - Mega dropdown columns: gap-12 between columns, py-10 top/bottom padding
   - Campaign image in dropdown: my-6 top/bottom margin, never touch the dropdown edges

4. HERO SECTION:
   - Text block: max-w-2xl, centered, with gap-6 between eyebrow / headline / subtitle / CTAs
   - CTA buttons: px-8 py-3.5, gap-4 between them
   - Never let hero text touch the viewport edges — px-6 minimum on mobile

5. CATEGORY GRID:
   - Grid gap: gap-6 desktop, gap-4 mobile
   - Card inner padding (for text overlay): p-6
   - Section wrapper: py-24 top and bottom

6. PRODUCT CARDS:
   - Image container: aspect-[3/4] enforced with overflow-hidden
   - Card text area below image: pt-3 pb-1 space-y-1
   - Product name: text-sm font-medium
   - Price: text-sm, separate line, gap from name
   - Quick-add button: px-4 py-2, full width of card, never overlap the image badly
   - Grid: 4 cols desktop (gap-6), 2 cols tablet (gap-4), 1 col mobile

7. PRODUCT DETAIL PAGE:
   - Left/right split: gap-12 between image gallery and info panel
   - Info panel internal spacing: space-y-6 between each element group
   - Size buttons: gap-2, min-w-[44px] h-[44px] for touch targets
   - Color swatches: gap-3, w-8 h-8 each
   - Add to Bag button: py-4, full width, mt-6 from size selector
   - Accordion items: py-4 each, border-b between them

8. COLLECTION PAGE:
   - Sidebar width: w-64, shrink-0, pr-8 padding from grid
   - Filter group spacing: space-y-6 between filter groups, space-y-2 between items within a group
   - Filter section heading: mb-3, text-xs uppercase tracking-widest
   - Product grid: flex-1, gap-6

9. CART DRAWER:
   - Drawer width: max-w-[420px] w-full
   - Header: h-16 px-6, border-b
   - Each cart item: py-4 px-6, gap-4 between image and text, border-b between items
   - Cart item image: w-20 h-24 object-cover rounded-md
   - Footer/checkout area: p-6, border-t, space-y-3

10. FOOTER & NEWSLETTER:
    - Newsletter section: py-24, max-w-xl centered content
    - Input + button row: gap-3, input flex-1, button px-6 py-3
    - Footer columns: gap-12 between columns, py-16 top padding
    - Footer link spacing: space-y-3 between links
    - Bottom bar: py-4, border-t border-white/10, flex justify-between

11. MARQUEE STRIP:
    - py-4 top and bottom padding
    - gap-8 or more between repeating text items
    - Font: text-sm uppercase tracking-widest

12. GLOBAL CONTAINER:
    - Every section must have: mx-auto max-w-[1440px] px-6 lg:px-12
    - No section should bleed full-width text — always a container wrapper
    - Check for any element using w-full without a max-width parent — fix it

13. MOBILE AUDIT (375px breakpoint):
    - All grids collapse correctly (4col→2col→1col)
    - No horizontal overflow anywhere — add overflow-x-hidden to layout root if needed
    - All touch targets minimum 44x44px
    - Section padding halves on mobile (py-16 instead of py-24)
    - Typography scales down: hero headline 3xl max on mobile, section headings 2xl max

14. FLEXBOX & GRID FIXES:
    - Replace any flex container missing items-center that looks misaligned
    - Replace any grid missing gap that looks congested
    - Any absolute-positioned element: check it doesn't overlap siblings unexpectedly
    - Check all z-index layers: navbar z-50, mobile menu z-60, cart drawer z-50, overlays z-40

After fixing all spacing, do a final visual pass and ask: 
"Does every section have room to breathe? Is there at least 24px of space between any two distinct UI elements? Does the page feel airy and premium rather than congested?"

If the answer is no for any component, fix it before moving on.

═══════════════════════════════════════
PART 2 — PERFORMANCE
═══════════════════════════════════════

- All images: use Next.js <Image> with priority on hero, lazy on rest, placeholder="blur"
- Wrap client-heavy components in next/dynamic with ssr:false
- Add loading skeletons for any async data sections

═══════════════════════════════════════
PART 3 — ANIMATION CONSISTENCY
═══════════════════════════════════════

- Create /components/ui/FadeInWhenVisible.tsx — a reusable wrapper:
  opacity 0→1, y: 40→0, duration 0.6s, ease: [0.25, 0.1, 0.25, 1]
- Apply it to every section block on the homepage
- All buttons: whileTap={{ scale: 0.97 }}
- All product images: overflow-hidden + group-hover:scale-105 transition-transform duration-500

═══════════════════════════════════════
PART 4 — SEO & FINAL QA
═══════════════════════════════════════

- Add metadata exports to every page.tsx
- Lock body scroll when mobile menu or cart drawer is open
- Report every file you changed and what spacing/layout fix was applied to it